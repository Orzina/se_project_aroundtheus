export default class Section {
  constructor({ items, renderer }, cardSelector) {
    this._pageElement = 
  }

  renderItems() {
    this._pageElement.forEach(page () => {
      renderer()
    })
  }

  addItem()
}
